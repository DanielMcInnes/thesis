
with "aws";

project OCPPServer is

   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Exec_Dir use ".";

   for Main use ("ocppserver-main.adb");
   --for OCPPWebsocket use ("ocpp-websocket.adb");

   package Builder is
      for Executable ("ocppserver-main.adb") use "ocppserver";
   end Builder;

end OCPPServer;
